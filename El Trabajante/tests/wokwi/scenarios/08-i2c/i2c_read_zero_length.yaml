# I2C Read Zero Length Test
# Test-ID: I2C-COMM-006
# Validates: readRaw() returns false for length == 0
# Expected: Error logged, function returns false
# Timeout: 90000ms

name: I2C Read Zero Length (I2C-COMM-006)
version: 1
steps:
  # I2C Bus Manager initializes
  - wait-serial: "I2C Bus Manager initialized successfully"

  # System operates normally
  - wait-serial: "Phase 4: Sensor System READY"

# NOTE: This test validates parameter checking in readRaw()
# Implementation (i2c_bus.cpp):
#   if (buffer == nullptr || length == 0) {
#       LOG_ERROR("I2C read: Invalid buffer or length");
#       return false;
#   }
#
# Mock test required: test_i2c_bus.py
# Call: readRaw(0x44, 0x00, buffer, 0)
# Expected: Returns false
