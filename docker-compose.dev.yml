# AutomationOne - Development Overrides
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d

services:
  el-servador:
    environment:
      LOG_LEVEL: DEBUG
      SERVER_RELOAD: "true"
      ENVIRONMENT: development
    volumes:
      - ./El Servador/god_kaiser_server/src:/app/src
      - ./El Servador/god_kaiser_server/alembic:/app/alembic
      - ./El Servador/god_kaiser_server/tests:/app/tests
      - ./logs/server:/app/logs
    command: ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload", "--reload-dir", "/app/src"]

  el-frontend:
    environment:
      NODE_ENV: development
    volumes:
      - ./El Frontend/src:/app/src
      - ./El Frontend/public:/app/public
      - ./El Frontend/index.html:/app/index.html
      - ./El Frontend/vite.config.ts:/app/vite.config.ts
      - ./El Frontend/tsconfig.json:/app/tsconfig.json
      - ./El Frontend/tailwind.config.js:/app/tailwind.config.js
      - ./El Frontend/postcss.config.js:/app/postcss.config.js
